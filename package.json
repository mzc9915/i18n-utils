{
  "name": "i18n-utils",
  "version": "0.1.0",
  "private": true,
  "description": "i18n utilities for extracting and managing translations",
  "scripts": {
    "dev": "pnpm --filter extract dev",
    "build": "pnpm --filter extract build",
    "test": "pnpm --filter extract test"
  },
  "keywords": ["i18n", "vue", "extract", "translation"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}

